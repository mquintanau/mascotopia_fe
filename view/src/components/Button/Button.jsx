import React from "react";

const Button = () => {
  return (
    <button className="rounded-lg bg-primary px-4 py-1 text-black hover:bg-main hover:text-primary">
      Button
    </button>
  );
};

export default Button;
